# Goal-Loop 示例 — Ontology 覆盖率冲刺

目录：`.hermes/`（项目根）

## GOAL 摘要

- **总目标：** 覆盖率 ≥80% + `ci:check` 全绿
- **选定策略示例：** S2 测试增量优先（并行 GS-01~04，再 GS-05/06 门禁）

## GOALS 并行标注示例

```
GS-01 (store UI) ─┐
GS-02 (lib) ──────┤
GS-03 (components) ─┤──→ GS-05 (ci:check) ──→ GS-06 (coverage+push)
GS-04 (API) ──────┘
```

每个 GS 含：完成标准 · 回溯条件（如「改 store 行为」）· 迭代预算（如 3 轮）

## REPORT 状态表示例

| 编号 | 名称 | 状态 | 进度 | 迭代次数 |
|------|------|------|------|----------|
| GS-02 | lib 纯函数 | 已完成 | 100% | 1 |
| GS-04 | API 路由 | 已完成 | 100% | 1 |
| GS-03 | 组件测试 | 未开始 | 0% | 0 |

## 决策与实验记录示例

| 日期 | 编号 | 类型 | 内容 | 结果 | 原因分析 |
|------|------|------|------|------|----------|
| 2026-06-27 | D-01 | 决策 | parse-pptx 单测 vs 仅路由 mock | 跳过 lib 单测 | 未安装 markitdown，Vite 无法 resolve |
| 2026-06-27 | E-01 | 实验 | export POST 用 minimalProject | 失败 500 | 改用 createFrozenProject 通过 |

## 回溯触发示例

**条件：** 「为凑覆盖率修改业务逻辑」  
**动作：** GS-02 → `需回溯`，修订 GOAL 禁止事项，暂停 GS-03
